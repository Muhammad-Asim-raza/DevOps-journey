=== LINUX CAPABILITIES ===
--- Default capabilities (too many) ---

--- Drop all caps (minimal) ---
bash: line 2: ping: command not found
Container still runs with no capabilities

--- Secure production flags ---
docker run -d \
  --read-only \
  --cap-drop ALL \
  --cap-add NET_BIND_SERVICE \
  --security-opt no-new-privileges:true \
  --tmpfs /tmp:rw,noexec,nosuid,size=64m \
  --memory 256m \
  --cpus 0.5 \
  myapp:v1.0
